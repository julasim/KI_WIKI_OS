# Bot v2 — Telegram + Whisper + LLM-API + MCP-Client. Nur diese 4 Layer.

# Telegram + JobQueue (Reminders)
python-telegram-bot[job-queue]>=21.0

# LLM-API (OpenAI-kompat: Anthropic / OpenRouter / Ollama)
openai>=1.50

# Voice → Text (lokales Whisper)
faster-whisper>=1.0

# Document-Upload: PDF + DOCX Text-Extraktion (Bot extrahiert, MCP speichert)
pymupdf>=1.24
python-docx>=1.1

# MCP-Client (HTTP-Session, Streamable-HTTP zum Vault-Server)
mcp>=1.2
httpx>=0.27

# Frontmatter (fuer pending-state in 06_Meta/bot-memory/)
python-frontmatter>=1.1
